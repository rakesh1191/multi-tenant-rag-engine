apiVersion: v1
kind: ServiceAccount
metadata:
  name: rag-engine-api
  namespace: rag-engine
  labels:
    app: rag-engine-api
  # Cloud-specific annotations added via overlay:
  # AWS: eks.amazonaws.com/role-arn
  # GCP: iam.gke.io/gcp-service-account
